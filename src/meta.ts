import { MyTypes } from "./declarations";

export const meta = {
  assets: null as null | MyTypes["Assets"],
  repond: null as null | MyTypes["Repond"],
  stores: null as null | MyTypes["Stores"],
};
